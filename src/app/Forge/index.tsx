import React from 'react';
import { Text } from 'react-native';
import { MainBody } from '../../core/components/containers';

export const Forge = () => {
  return (
    <MainBody>
      <Text style={{ color: 'black' }}>index</Text>
    </MainBody>
  );
};
